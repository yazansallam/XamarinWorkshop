<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Quiz.MainPage">


    <StackLayout>
        <Frame BackgroundColor="#2196F3" Padding="24" CornerRadius="0">
            <Label Text="Quiz" HorizontalTextAlignment="Center" TextColor="White" FontSize="36"/>
        </Frame>
        <Button Text="Statistic" Clicked="Button_Clicked" />
        <Label Text="{Binding MeineFrage}" FontSize="16" Padding="30,0,30,0"/>
        <Button Text="Falsch" Command="{Binding FalschCommand}"/>
        <Button Text="Richtig" Command="{Binding RichtigCommand}"/>
        <Label Text="{Binding Ergebniss}" FontSize="16" TextColor="{Binding MeinColor}" Padding="30,0,30,0"/>
        <Button Text="Überspringen" Command = "{Binding ÜberspringenCommand}"/>
        
    </StackLayout>

</ContentPage>
